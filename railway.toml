[build]
  # Install root (backend) dependencies - Updated build config
  nixPkgs = ["nodejs_20"]
  builder = "NIXPACKS"

[deploy]
  startCommand = "npm start"
  healthcheckPath = "/api/tenders"

[[services]]
  name = "security-tender-evaluator"
  protocol = "TCP"
  internal_port = 5000
