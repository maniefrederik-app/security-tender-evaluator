# Database — required
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Port (Railway sets this automatically)
PORT=5000

# CORS allowed origins (comma-separated, leave blank to allow all)
ALLOWED_ORIGINS=
